<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pbsi2.instruments">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.pbsi2.instruments.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.pbsi2.instruments.GuitarsActivity"
            android:label="@string/category_Guitars"
            android:parentActivityName="com.pbsi2.instruments.MainActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.pbsi2.instruments.MainActivity" />
        </activity>

        <activity
            android:name="com.pbsi2.instruments.PercussionsActivity"
            android:label="@string/category_Percussions"
            android:parentActivityName="com.pbsi2.instruments.MainActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.pbsi2.instruments.MainActivity" />
        </activity>

        <activity
            android:name="com.pbsi2.instruments.KeyboardsActivity"
            android:label="@string/category_Keyboards"
            android:parentActivityName="com.pbsi2.instruments.MainActivity">
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.pbsi2.instruments.MainActivity" />
        </activity>

    </application>

</manifest>